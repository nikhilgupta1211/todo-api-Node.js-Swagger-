## Api for todo-list in Node.js(Express) + Swagger

### Steps to start
First edit the `config.json` file located in `api/config`.
Then start the server with

```
swagger project start
```

Basic Authentication:
```
Username: nikhil
Password: nikhil
```